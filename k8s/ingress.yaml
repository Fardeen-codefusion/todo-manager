apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-manager-ingress
  namespace: todo-app
spec:
  ingressClassName: nginx
  rules:
    - host: todo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-manager-service
                port:
                  number: 80
